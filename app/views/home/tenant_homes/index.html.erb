
<p id="notice"><%= notice %></p>

<h1>Listing home / tenant</h1>

<table>
  <thead>
  <tr>
    <th>Home</th>
    <th>tenant</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @tenant_homes.each do |tenant_home| %>
    <tr>
      <td><%= tenant_home.home_id %></td>
      <td><%= tenant_home.tenant_id %></td>
      <td><%= link_to 'Show', home_tenant_home_path(tenant_home.id) %></td>
      <td><%= link_to 'Edit',  edit_home_tenant_home_path(tenant_home.id) %></td>
      <td><%= link_to 'Destroy', home_tenant_home_path(tenant_home.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New tenant', new_home_tenant_home_path %>x