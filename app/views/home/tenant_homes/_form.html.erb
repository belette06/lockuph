<%= form_for [:homes, @tenant_home] do |f| %>
  <% if @tenant_home.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tenant_home.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
        <% @tenant_home.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>@tenant_home
  <% end %>

  <div class="field">

    <%= f.fields_for :tenant do |tenant| %>
    <%= tenant.label :tenant%><br />

    <% end %>
  </div>




  <div class="actions">
    <%= f.submit %>
  </div>

<%end %>